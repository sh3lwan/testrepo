{
  "name": "fastifytest",
  "version": "1.0.0",
  "description": "A simple Fastify server with Sudoku game",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc && cp -r src/public dist/",
    "start": "node dist/app.js",
    "dev": "npm run build && node dist/app.js"
  },
  "keywords": [
    "fastify",
    "sudoku"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.25.2",
    "@fastify/static": "^6.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3"
  }
}
